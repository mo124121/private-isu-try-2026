all:
  vars:
    tailscale_a_records: # ansibleの実行用
      S1: private-isu1
      B: private-isu2
    internal_ips: # 内部IP サービス間の相互接続に使用
      S1: 172.31.9.15
      B: 172.31.3.119
    nginx_ip: "{{ internal_ips.S1 }}"
    db_ip: "{{ internal_ips.S1 }}"
    main_web_ip: "{{ internal_ips.S1 }}"
    db_user_hosts: []
    webapp_deploy_dir: /home/isucon/private_isu/webapp/golang
    webapp_binary_name: app
    benchmarker_workdir: /home/isucon/private_isu/benchmarker
    benchmarker_bin: /home/isucon/private_isu/benchmarker/bin/benchmarker
    benchmarker_userdata: /home/isucon/private_isu/benchmarker/userdata
    benchmark_target_url: "http://{{ nginx_ip }}"
    ansible_user: ubuntu
  children:
    target:
      children:
        nginx:
          hosts:
            nginx01:
              ansible_host: "{{ tailscale_a_records.S1 }}"
        webapp:
          hosts:
            webapp01: # 複数デプロイする場合はwebapp02という感じで増やす
              ansible_host: "{{ tailscale_a_records.S1 }}"
        db:
          hosts:
            db01:
              ansible_host: "{{ tailscale_a_records.S1 }}"
    outsider:
      hosts:
        benchmarker:
          ansible_host: "{{ tailscale_a_records.B }}"
